# 黄蓝开发沟通：<功能名称>

> 本文档按时间从上往下追加。黄区 AI 只处理最下方状态为“等待黄区分析”的轮次；历史轮次不得删除或重写。

## 固定职责

- 蓝区 AI：分析共享代码、实施最小修改、设计诊断日志、根据黄区回报作出下一步判断。
- 用户：构建、安装、启动环境并人工执行真实操作。
- 黄区 AI：读取本文档和指定日志，分析最新问题并生成可复制给蓝区的简短回报。

## 基础信息

- 目标：<用户可见目标>
- 分支与提交：<branch / commit / 未提交状态>
- 关键源码：<文件及职责>
- 环境限制：<蓝区无法独立验证的原因>
- 身份分层：
  - 源码：<路径/版本>
  - 构建产物：<路径/版本或待确认>
  - 安装文件：<路径/版本或待确认>
  - 当前进程：<路径/版本或待确认>

---

## 第 01 轮：<简短问题名>

- 状态：等待黄区分析
- 当前唯一问题：<本轮必须回答的一个问题>
- 蓝区修改：<改动和原因>
- 预期调用路径：<入口 -> 边界 -> 结果>
- 需要检查的日志标记：
  - `[feature][R01-01]`：<应证明什么>
  - `[feature][R01-02]`：<应证明什么>
- 用户已执行的操作：<由用户填写或转述；黄区 AI 不执行>
- 黄区检查范围：<日志路径、时间范围、会话 ID 等非敏感定位信息>
- 结论判定：
  - 若 <证据 A>：说明 <结论 A>
  - 若 <证据 B>：说明 <结论 B>
- 停止条件：<找到首个断点或回答当前问题后立即停止>

### 黄区 AI 固定回复模板

黄区 AI 只在聊天中输出代码块内这些行，不编辑本文档。日志没有提供的值填写“未确认”，不使用源码或推断补齐。不得增加标题、解释、修复建议或“蓝区结论”；输出最后一行“结论”后立即停止。

```text
轮次：第 01 轮
日志序列：R01-01=<有/无>，R01-02=<有/无>
运行身份：<仅填写日志直接证明的路径/版本/进程；日志未提供则填“未确认”>
结果：<成功/失败/未到达>
首个有效错误或断点：<一行；没有则填“无”>
结论：<只写一个由本轮运行日志直接支持、回答当前问题的结论>
```

### 黄区回报

<收到后由蓝区追加原始简短回报；等待期间保留为空>

### 蓝区结论

<收到回报后由蓝区追加判断，并把本轮状态记为“已分析”>

---

<!-- 后续轮次复制上方结构，递增轮次和固定日志编号，并追加到文件底部。 -->
